# AI 问书 · 三端功能清单（开发依据）

> 版本：评审定稿 v1 · 日期：2026-06-01
> 范围：机构前台 H5 / 机构后台 Web PC / 平台超管 Web PC，三端高保真原型（只 mock，不接真后端）
> 说明：本清单为产品评审多轮确认后的定稿，作为原型开发的功能依据。每个功能点均含功能描述。

---

## 一、机构前台 H5（C 端用户）

### 1. 登录与权限

| 功能点 | 功能描述 |
|---|---|
| 登录（微信 / 手机号两种方式） | 落地页仅展示 AI 问书 icon + slogan + 两个登录按钮（不展示「你好我是…AI 版作者」问候语）。① 微信登录：授权获取 openid，登录成功后若该微信号未绑手机号则引导输入手机号 + 短信验证码绑定（提供「暂不绑定」可跳过直接进首页），已绑则直接进首页；若手机号在本机构已存在但未绑微信，通过**手机号 + 验证码**校验本人，通过则合并绑定，失败则清空并回到手机号输入步骤。② 手机号登录：手机号 + 短信验证码直接登录。 **【0613】按 H5 实际能力重构：(a) 微信网页授权（snsapi_userinfo）可取 openid / 昵称 / 头像 / 性别 / 地区，但 H5 公众号网页无法获取手机号（微信一键授权手机号为小程序专属能力），故手机号统一用「手机号 + 验证码」绑定，不分浏览器环境；(b) 删除「账号合并」逻辑——微信欲绑的手机号若在本机构已被占用，直接提示「该手机号已被其他账号绑定，请更换或联系平台客服」，不做合并（A、B 两个微信绑同一手机号亦同此提示）；(c) 手机号验证码登录的用户不获取 / 绑定微信信息。** |
| 微信授权登录（两种场景） | 以**微信内置浏览器打开 H5** 为主路径，按打开环境分两种授权交互：① **微信 App 内打开 → 专属授权弹窗**：点击「微信授权登录」**不出二维码**，直接唤起微信官方授权弹窗，询问是否授予昵称、头像权限；点「允许」携 `code` 走回调——新用户引导绑定手机号、已绑手机号用户直接进入 AI 会话页，点「拒绝」留在登录页。② **外部浏览器打开 → 微信扫码授权页**：由微信域名 `open.weixin.qq.com` 渲染**动态二维码**，用户微信扫码后手机端弹出授权确认（允许 / 拒绝）；「允许」携 `code` 跳转回调页继续①的绑定 / 进入流程，「拒绝」直接退回登录页。 **【0610】落地页移除「在浏览器中打开？扫码登录」入口（不在原型上绘制），外部浏览器扫码授权场景仅在本清单保留说明。** **【0613】两种环境授权流程在原型上均绘制并可切换演示**（落地页提供「演示环境：微信内 / 浏览器」切换）：微信内 → 官方授权弹窗（授权项更新为昵称 / 头像 / 性别 / 地区）；浏览器 → `open.weixin.qq.com` 扫码授权。授权回调自动带回头像 / 昵称 / 性别 / 地区写入个人资料；新用户可绑手机号或「暂不绑定」直接进入。 **【0613-2】非微信浏览器扫码登录页采用内嵌二维码（WxLogin JS SDK，整页可自定义美化，仅二维码图由微信生成）；二维码中央不再放 AI 问书 Logo（二维码属微信、避免误导）。演示环境切换控件移至「用户协议 / 隐私政策」下方，并加「仅用于演示 · 环境切换，非上线功能」标识。** |
| 二维码失效落地页 | 二维码作废 / KP 下架时弹窗提示「二维码失效，即将跳转至 AI 问书首页」，倒计时 3 秒自动跳首页。 |
| 游客模式 | 未登录可浏览只读内容；创建会话、发送问题等需授权操作时引导登录，登录成功后自动补发游客态已输入问题；左下角「我的」未登录态点击亦触发登录。 |
| 登录态有效期 | 登录态有效期 30 天，每次操作自动重置有效期。 |
| 敏感操作手机号绑定门槛（合规） | **【0613】新增。** 微信登录用户可不绑手机号使用基础功能；但**支付下单、兑换码核销、开通 / 充值会员**三类操作前置校验是否已绑手机号，未绑则弹窗引导前往「我的 - 绑定手机号」完成后继续（不强制所有用户绑定，仅在交易 / 权益变更前要求）。 **【0613-2】拦截弹窗文案排成两行并整体垂直居中（「根据相关规定，」一行、「支付下单、兑换码与开通会员需先完成手机号绑定。」一行）。** **【0614】拦截弹窗文案改为「支付、开会员等操作需绑定手机号，来保障账户安全」；会员续费同样前置此校验——未绑先引导绑定再续费（自动续费前需短信通知用户）。** |

> **【0613 登录与手机号绑定 · 详细规则（PRD 依据）】**
>
> **A. 微信网页授权可获取的信息**
> - 经微信网页授权（scope=`snsapi_userinfo`）可获取：`openid`、`unionid`、**昵称、头像、性别、地区（国家 / 省 / 市）**。
> - **不可获取手机号**：H5 公众号网页授权无手机号能力（微信「一键授权手机号」为小程序 `getPhoneNumber` 组件专属，H5 不具备），故本产品手机号一律由用户手动输入「手机号 + 验证码」绑定。
>
> **B. 登录流程（按打开环境分流）**
> - **B1 微信内置浏览器**：点「微信授权登录」→ 唤起微信官方授权弹窗（授权项：昵称 / 头像 / 性别 / 地区）→ 点「允许」携 `code` 回调，拉取上述资料写入账户 → 进入 AI 会话首页（手机号为空，后续按需绑定）；点「拒绝」→ 留在登录落地页。**不出二维码，且不再弹「获取手机号」授权窗（H5 不具备该能力）。**
> - **B2 非微信浏览器**：点「微信授权登录」→ 由微信域名 `open.weixin.qq.com` 渲染动态二维码 → 用户微信扫码、手机端确认「允许 / 拒绝」→「允许」携 `code` 回调，流程同 B1 → 进入首页。
> - **B3 手机号验证码登录（两环境一致）**：输入手机号 + 短信验证码，校验通过即登录。**此方式不获取、不绑定任何微信信息**（手机号登录用户无需微信授权）。
> - 落地页提供「演示环境：微信内 / 浏览器打开」切换——**仅原型演示用**，生产环境由 UserAgent（是否含 `MicroMessenger`）自动判定，无此切换。
>
> **C. 头像 / 昵称 / 性别 / 地区 的来源**
> - 微信环境：登录授权时自动带回，个人资料页只读展示并标注「来自微信」。
> - 非微信环境：个人资料页支持用户**上传头像、自行填写性别与地区**。
>
> **D. 手机号绑定（统一「手机号 + 验证码」，不分环境）**
> - 微信登录用户：登录后引导绑定，可「暂不绑定」直接进入，后续在「我的 - 绑定手机号」补绑。
> - 绑定 / 换绑均只需「（新）手机号 + 验证码」，**不要求旧手机号双重校验**（产品无密码凭证，换绑常因旧号停用；登录态本身即归属凭证）。
>
> **E. 手机号绑定冲突（不做账号合并）**
> - 待绑手机号在本机构已被其他账号 / 微信绑定 → 提示「该手机号已被其他账号绑定，请更换其他手机号，如有疑问请联系平台客服」，**不合并、不顶替**，引导更换手机号或联系客服。**【0613-2】占用提示页「联系平台客服」复用「我的 - 联系客服」同款弹窗（含客服二维码 / 电话 / 邮箱），不另行绘制。** **【0614】占用提示文案「联系平台客服」统一改为「联系客服」。**
> - A 微信、B 微信先后绑定同一手机号：后者同样命中占用提示（不合并）。
> - 待绑手机号在本机构唯一 → 正常绑定成功。
>
> **F. 敏感操作手机号绑定门槛（合规）**
> - 微信登录用户可不绑手机号使用基础功能；但**支付下单、兑换码核销、开通 / 充值会员**三类操作前置校验是否已绑手机号，未绑则弹层引导前往「我的 - 绑定手机号」，完成后继续。不强制全体绑定，仅在交易 / 权益变更前要求。

### 2. AI 会话

| 功能点 | 功能描述 |
|---|---|
| Agent 形象 sticky 区 | 会话顶部固定 Agent 形象，会话区下滚渐隐隐藏、上滚复原显示。 |
| Agent 路由（一次性判定） | 扫码或 H5 链接均带 KP ID；首次进入会话按 KP ID 拉绑定 Agent 渲染形象 / 声音，并在 slogan「有问题，就问 AI 问书」下方展示系统基于 KP 自动提炼的 3 条入门示例问题（点击即发送）；后续提问不再重判定。 |
| 输入框能力开关（深度思考 / 智能搜索） | 会话输入框下方提供两个相互独立的能力开关：**深度思考**——开启后调用更强推理、给出更细致的思考型回答，**默认开启**；**智能搜索**——开启后允许联网检索补充知识库未覆盖的信息并标注来源，**受机构「联网配置」总开关约束**（该总开关由平台超管在「机构详情 / 机构配置 / 联网配置」中设定，机构未开启联网时此开关不生效），**默认关闭**。两开关均可单独切换，状态随会话保留。视觉状态：**未选中＝白底 + 黑字 + 黑 icon；选中＝主题色（靛蓝软底 + 靛蓝字 / icon）**。 |
| 流式答案 + 多模态卡片 | 文字打字机式流式输出（**仅新生成的回答展示打字机动效；切换历史会话、实时电话挂断返回等场景下，已生成的回答直接整段展示、不重复动效**）；卡片内**文字区在上、媒体区（图 / 音 / 视）在文字区下方紧凑排布**，两区做明显视觉区分；媒体并列排布支持左右滑动，**顺序不固定，按后台返回的相关度从左到右排列**（非图→音→视固定序）；受限内容按权益打锁标；点击任一媒体可放大。 |
| 答案操作（复制 / 点赞 / 反馈） | 每条 AI 答案下方、「参考 N 篇知识」入口左侧并排展示三个操作 icon：复制（复制答案文本）、点赞、反馈（点踩 / 提交问题反馈）。 **【0610】点赞 / 反馈 icon 改灰色系描边填充样式（选中态为灰色填充、非主题色），参考图 1。** |
| 输入框语音输入 | 麦克风按钮：**单击 / 长按进入语音输入态**——在**当前会话页底部弹出音浪动画浮层**（非全屏新页；浮层背底渐变与会话背景自然过渡、不割裂）；点击音浪外任意位置即关闭并发送本次语音问题、触发 AI 回答。**【0610】新增。** |
| 回答溯源富信息 | 答案下展示「参考 xx 篇知识」入口，点击后从右侧抽屉拉出溯源列表，**仅按相关度从高到低排序、不展示相关度数值（排序规则仅研发依据，界面不显示文案）**。**分两块展示，中间用浅灰横线分隔**：上块为知识 KP（每条含 KP 封面图 48×60 + KP 名称 + 文件名 + 章节页码；**同一 KP 内命中多个文件时归并在该 KP 下逐条列出，最多展示 2 个，其余以「…等 N 个文件」省略**），下块为互联网检索内容（站点用**小圆形图标**表示，不再用书本矩形封面）。字段过长尾部省略号截断。条目点击行为：book 类型跳转该 KP 的纸书购买链接，链接为空则提示「**暂不可查看详情**」（3 秒后自动消失，提示居屏幕中上方）；web 类型直接打开来源网页。 **【0613】web 类型来源跳转第三方前增加 3 秒倒计时过渡浮层「即将跳转至第三方页面…」，倒计时结束再打开来源网页。** **【0613-2】倒计时浮层扩展到所有「跳出 AI 问书」的链接——web 互联网来源 + 知识 KP 的纸书 / 运营填写链接均先弹 3 秒过渡浮层；浮层不再展示具体 URL 地址（仅文案提示）。** **【0614】跳转过渡浮层改为黑底白字「倒计时条」样式（左侧 3·2·1 倒计时 + 「即将跳转到外部」+ 右侧「取消跳转」按钮），不再用大弹窗。** **【0614c】倒计时条移到**屏幕中上方**（与 3 秒 toast 同位）；触发跳转时**不再关闭右侧「参考来源」抽屉**，直接在当前页（抽屉之上）给提示。** |
| 媒体资源区与付费墙时机 | 答案卡片内「**相关媒体资源**」作为加粗小标题直接贴在卡片上（不再单独灰底框），不展示「按相关度排序」文案。付费墙两种触发时机：① 在会话内**直接点击需付费的媒体资源** → 直接弹出付费墙（不进放大预览）；② 点击免费 / 已付费资源进入放大预览后**翻页浏览到需付费内容** → 在放大预览界面**下方**弹出付费墙（盖在预览之上，不被遮挡）。付费墙去除左侧锁图标，标题 / 描述 / 按钮整体居中。 |
| 推荐追问 | 展示在多模态卡片下方，2–3 个追问**上下纵向堆叠（非左右并列）**，点击即发送。 |
| 多模态全屏 lightbox + 版权保护 | 放大后顶部固定「因版权限制，暂不支持保存下载」；底部缩略图条可点击切换，主预览区支持左右滑动浏览同卡片其他媒体。 |
| 实时电话式语音 | **会员专属能力**：未开通会员点击电话入口时引导开通；开通后方可进入。独立电话页：中央 Agent 形象，下方居中「挂断」（返回原会话并定位最近一轮），左侧「静音」（关闭时不收音），右侧「字幕」（开启时显示字幕、Agent 形象隐藏，关闭时反之）。挂断按钮上方展示实时语音状态文案 + 动画（如「正在听…」「正在回答…」）。字幕开启时对话文本按交互时序自上而下、左对齐排列，用户文本浅灰色、AI 回复深黑色。全程问答同步写入 AI 会话历史。 |

### 3. 付费墙与权益

| 功能点 | 功能描述 |
|---|---|
| AI 会员订阅页 | 仅展示首月折扣价、月度价两档，无年付；含自动续费协议入口与 72 小时宽限期说明。 **【0613】开通 / 充值会员前校验手机号绑定（见「敏感操作手机号绑定门槛」）。** **【0614】会员「续费 / 开通」前先校验手机号绑定，未绑则先引导绑定再继续，不直接进入自动续费。** |
| 永享购买 | 触发受限内容时弹出永享购买卡，展示对应图 / 音 / 视单价与权益说明（永久解锁该单条内容）。 **【0613】支付下单前校验手机号绑定。** |
| 兑换码兑换 | 「我的 - 兑换码」入口核销会员权益，支持手动键入与调起相机 / 相册 OCR 拍照识别，需获取摄像头 / 相册权限。 **【0613】核销前校验手机号绑定。** |
| 微信支付过场 + 结果页 | **会员开通与永享买断统一走模拟微信支付收银台**（**标题区白底、与下方区域一致；AI 问书 logo 用会话同款电光靛紫色动画球**；商户、金额、零钱支付方式、确认支付按钮沿用微信绿、模拟环境提示）→ 确认 → 成功结果页。成功页为品牌质感设计（**支付成功对勾用绿色 / 主题色 + 同心圆光环，替代原珊瑚 / 朝霞橙，避免被误解为未支付成功**），「返回会话」**定位到支付前掉起支付的那个会话**（非新会话）。 **【0615】支付收银台补「微信内 / 微信外」双场景，随落地页环境开关联动：微信内走 **JSAPI（公众号支付）**，零钱 / 快捷直接在微信内调起；微信外走 **H5 / MWEB 支付**，提示「即将唤起微信完成支付」并模拟跳转 → 回跳结果页。两种方式 UI 与文案分别呈现，完整演示微信外支付路径。** |

### 4. 我的

| 功能点 | 功能描述 |
|---|---|
| 账户管理 | 仅保留「换绑手机号」（新号 + 验证码校验）。 **【0613】确认换绑仅需「新手机号 + 验证码」，不要求旧手机号双重校验**（产品无密码凭证、换绑多因旧号停用；登录态本身即归属凭证，异常场景留待生产期风控 / 客服申诉）。 |
| 用户协议 | 独立入口，纯文本 / Markdown 渲染。 |
| 隐私政策 | 独立入口，与用户协议并列。 |
| 联系客服弹窗 | 展示机构后台配置的一个二维码（企微 / 个微），下方电话、邮箱均非必填，有则展示无则隐藏并自动补位（不留空块）。 |
| 会员中心 | 非会员展示会员权益 + 立即开通入口；会员展示当前权益、到期时间、权益明细、「取消自动续费」入口（续费为自动，无手动续费按钮）。 **【0613】(a) 非会员页改为「免费 vs 会员」权益对比表（逐项 ✅ / ✗，永享标注为「单独购买」），强化付费价值感；(b) 开启自动续费后弹 3 秒消失 tips「将于到期前 3 天短信提醒」，权益说明同步标注「提前 3 天短信通知续费」。** **【0613-2】对比表首行「基础图文问答」改为「基础知识问答」（知识库文字内容免费 / 会员均免费）。** **【0614】会员权益名称统一为五项：基础文字知识问答 / 图音视频深度知识精讲 / VIP 极速优先通道 / 实时电话即时问答 / 永享名家典藏知识（会员中心与对比表同步）。** |
| 我的永享 | 已购永享列表，可点击放大预览（右上角圆形「×」可关闭）；不支持保存下载，顶部固定版权保护文案。**视频控件紧贴视频下方**（非页面最底），支持播放 / 暂停（暂停用 ⏸ 图标，非麦克风）、±15s 快进快退、进度条拖动、横竖屏切换。 **【0610】放大预览内容垂直方向调整为水平居中、略偏上（不再贴顶过高）。** **【0613】列表顶部新增搜索（按文件名模糊匹配）+ 文件类型筛选（全部 / 图片 / 音频 / 视频）。** |
| 我的订单 | 顶部四 Tab 切换：全部 / 会员 / 永享 / 兑换码；每单展示金额、状态、时间；仅「永享」订单展示关联 KP。订单详情按类型差异化：**会员**——支付方式下方增加「自动续费 / 手动支付」标识；**永享**——内容整行可点直接预览（无「点击封面预览」文字按钮）；**兑换码**——页面标题为「详情」（非订单详情），仅展示「类型：兑换码核销 / 兑换码 / 兑换时间」+ 兑换权益（权益名 + 会员时间区间），不展示订单编号 / 支付方式 / 下单付款时间 / 金额。兑换码输入框为真实输入框（有光标）。 |
| 我的纸书（扫码解锁的知识 KP） | **【0613】新增。** 展示用户通过**线下纸书二维码**扫码绑定、获得权益的知识 KP；卡片布局（KP 封面图 + KP 名称 + 扫码时间），支持按 KP 名称模糊搜索。权益：即使免费用户也可畅享该 KP 内全部文 / 图 / 音 / 视，**但「永享」标内容仍需单独购买**；与「我的永享」「我的订单」同级入口。 **【0613-2】(a) 列表说明文案改「微信扫码解锁纸书全部数字内容，永享内容另购」(尽量一行)；(b) 去掉卡片上的「永享」标签(本列表均为 KP 维度)；(c) 点击某本纸书直接进入该知识 KP 的 AI 会话主界面；(d) 卡片重排——KP 名称固定两行高度、「扫码解锁 · 时间」统一钉在卡片底部对齐(解决长短名称不齐不美观)；(e) 右上角新增「扫一扫」入口：点击调起手机相机扫码，扫到纸书二维码后直接进入对应 KP 的 AI 会话落地页并绑定该 KP 权益（新需求）。** **【0614】(a) 说明文案「永享内容另购」→「永享内容需另购」；(b) 右上角入口 icon 由相机改为「扫一扫」样式；(c) 扫码 / 点击纸书进入的是该 KP 的**全新会话**（非已有问答会话）；(d) 纸书卡片 hover 放大 / 高亮，提示可点击跳转。** **【0615】去掉纸书卡片 hover 放大 / 高亮交互（仅保留可点击，不再有视觉强调）；点击后在**屏幕中上方**弹 3 秒黑底 toast「即将进入「{KP 名称}」AI 会话」（与其他 3 秒提示同位），随后进入该 KP 全新会话。** **【0615-2】该 toast 限制最大宽度，文案过长（如长 KP 名）自动换行，不再超出屏幕。** |
| 个人资料（头像 / 昵称 / 性别 / 地区） | **【0613】新增。** 微信环境头像 / 昵称 / 性别 / 地区由微信授权自动带回（头像用微信头像）；非微信环境用户可在个人中心自行上传头像、填写性别与地区。 **【0613-2】(a) 取消「账户与帮助」内独立「个人资料」菜单，改由「我的」顶部头像 / 昵称 / 手机号区右侧箭头进入；(b) 全字段均可修改（微信仅作初始填充）：头像点击调起手机相册、上传后做圆形裁剪（新增裁剪页）；昵称行内可改并保存；性别仅「男 / 女」（去掉「保密」）；(c) 地区改为级联选择（省 / 市 / 区，采用真实中国行政区划数据，居中弹窗呈现、不手填，便于地区统计）；输入与「保存」之间保留间距。** **【0614】续修：头像裁剪去掉底部拖拽缩放条；顶部标题与取消 / 确定按钮套用前台 AI 主视觉重做（修复"模糊"问题）；点「确定」直接提示上传成功并在个人资料处回显；点「取消」回个人资料页（非「我的」页）。** |

---

## 二、机构后台 Web PC（机构运营）

### 1. 登录与主控台

| 功能点 | 功能描述 |
|---|---|
| 登录页 | 按 design 视觉稿实现，账号 + 密码（+ 可选验证码）。 **【0615】图形验证码支持点击刷新「换一张」（点击验证码或刷新按钮即随机重新生成 4 位字符，排除 0/O/1/I 等易混字符）。** |
| 个人中心（顶栏入口） | **【0615】新增。** 顶栏右上原「姓名 · 角色」下拉项改为「个人中心」，点击进入个人中心落地页（保留「退出登录」）。落地页展示：头像、账户名称、姓名、所属机构、上级机构（仅当所属机构有上级时显示）、角色、联系方式（手机 / 邮箱）、账户状态。**仅头像可上传更新**（相册选图 + 圆形裁剪，canvas 真裁剪输出小图，上传后顶栏头像同步回显）；账户名称 / 姓名 / 所属机构 / 角色 / 联系方式均**只读不可编辑**，表单底部灰色小字提示「若需更改账户信息，请联系平台管理员」。 |
| 主控台 Dashboard | 关键指标速览（DAU、当前会员数、累计 GMV、累计提问数）+ 最近 7 日趋势图 + 快捷入口。 **【0614】定位为「关键指标驾驶舱」，分两段：实时总览（累计 GMV / 累计退款·退款率 / **净 GMV 新增** / 当前会员数 / 累计注册用户，不随时间筛选）+ 经营分析（活跃用户 / 新增会员 / 区间 GMV / 区间提问数 + 趋势图，随今日 / 7 日 / 30 日联动）；移除「累计提问数」卡（深钻归数据看板·提问分析）。与数据看板分工：主控台=关键摘要快照，数据看板=分域拆解明细。** **【0615-6】主控台顶部「当前机构配额」卡升级为**当前生效订阅卡**（与平台后台机构详情同款 UI）：显示套餐（专业版 / 旗舰版…）/ 状态 / 加油包数 / 有效期 / 剩余天数 + KP / 存储 / 本月 Token「已用 / 上限（含加油包）」进度条；**机构侧只读——不显示商务负责人、不显示「新建订阅」按钮**。** |

### 2. Agent 人设系统

| 功能点 | 功能描述 |
|---|---|
| Agent 列表 | 机构 Agent 全机构唯一，机构创建时平台自动初始化默认头像 / 形象 / 声音；普通 Agent 用户可自由创建 / 编辑 / 删除，无数量限制。 |
| Agent 详情编辑 | 头像 / 形象支持 png/gif/jpg/jpeg；TTS 参考音上传作声音克隆素材；回答 Prompt 富文本（受权限点控制）；右侧 Tab 展示该 Agent 已关联 KP 列表，仅可查看不可编辑，并提供「前往 KP 详情修改关联」跳转入口（跳转到指定 KP 详情的基础信息 Tab 调整 Agent 绑定）；入门示例问题由系统按关联 KP 内容自动提炼 3 条，动态生成。 **【0613】移除「形象图」上传（头像保留，仅去形象图）；移除右下「AI 会话引导问题示例」区块。** |
| 回答 Prompt 编辑权限点 | 所有 Agent（内置 / 新建）均可被勾选该权限，默认不勾选；仅勾选后回答 Prompt 才可编辑。 |

### 3. 知识产品 KP

| 功能点 | 功能描述 |
|---|---|
| KP 列表（卡片布局） | 卡片展示封面图（9:16）+ KP 名称 + 来源标签（自建 / 共享）+ 状态 + 关键字段（关联 Agent、文件数、提问数等）；顶部「新建 KP」「导入分享 KP」按钮。 |
| 创建 KP 弹窗 | 输入 KP 名称等基础字段后创建；不在此选切片颗粒度（切片颗粒度针对知识库内单个文件，在上传文件时设定）。 |
| KP 详情 Tab 顺序 | 基础信息 → 知识库 → 永享 → 定价与权益 → 二维码 → 分享。 |
| 基础信息 Tab | 上传 KP 封面图（9:16，作 book 类型溯源图）；KP 名称、简介；纸书购买链接（非必填，问号 tooltip：「填写后，用户查看内容溯源时可跳转该链接」）；关联 Agent（下拉选本机构 Agent）；KP 状态以功能按钮切换：未发布 / 已发布 / 已下架 / 删除，发布、下架、删除均二次确认。删除为软删除（前期不做回收站，对用户等同永久删除）。仅「已发布」状态的 KP，其知识库内容才会被纳入用户问答检索；未发布 / 已下架 / 删除不参与检索。 **【0610】「关联 Agent」下拉框宽度收敛为与表单其他控件一致（详见 §四「后台下拉选择器」规范）。** |
| 知识库 Tab — 上传与浏览 | 无子 Tab；顶部入口为「**上传知识文件**」按钮，点击**居中弹出上传表单**：表单内设置切片方式，文件支持**点击上传 + 拖拽上传**、**支持多文件 / 文件 + 多媒体混传**（不支持文件夹嵌套上传），每个文件在表单内实时显示上传进度；全部上传完成后点「**进入知识库**」即进入解析、向量化处理（上传多个则在知识库列表展示多条）。列表支持按文件名（文档 / 图片 / 音频 / 视频）模糊匹配，支持按文件格式下拉筛选（文档 / 图片 / 音频 / 视频）。 **【0610】列表操作列最前增加「下载」按钮，可下载已上传的知识文件内容。** **【0610】上传弹窗切片方式字段与下方上传区域左对齐、上传拖拽区降高更紧凑；文件列表 PDF 文件 icon 中「PDF」三字母缩小并与边框留间距。** **【0613】去掉「切片方式」选择，直接上传**（不再让用户选章节 / 语义 / 字数，系统默认处理）。 **【0613-2】上传弹窗「点击选择 / 拖拽文件」区域改为弹窗内**居中**；并在上传区显示支持格式 / 大小 / 时长（见「知识文件格式」块），清晰告知用户。** **【0614】上传弹窗改**左右布局**：左上为选择 / 拖拽按钮（说明补「支持批量上传」），左下展示文 / 图 / 音 / 视格式与大小限制（并明确标注为**单个文件**上限，避免误解为单次合计），右侧展示批量上传进度与文件列表；同时删除知识库列表「切片方式」列；「向量化失败」问号 icon 与文字间距缩窄。** **【0615 / 0615-2】知识库列表文件类型小 icon 改用统一的彩色品牌图标（Word / PDF / 图片 / 音频 / 视频，两后台共用 FileTypeIcon 组件渲染）；icon 与文件名间距加大，便于一眼区分文件类型。** |
| 知识库 — 文档（Word/PDF） | 支持批量上传；用户选切片方式（章节 / 语义【推荐】 / 字数）+ 两个独立勾选项「解析文档内图片」「解析文档内表格」（勾选则对应内容一同向量化）。 **【0613】移除切片方式选择，仅保留「解析文档内图片」「解析文档内表格」两个勾选项。** |
| 知识库 — 纯图片 | 仅单独上传；用户手动填写图片含义文字后向量化。 |
| 知识库 — 纯音频 | 仅单独上传；系统自动提取字幕（提取耗时，弹窗内显示进度条并提示「正在提取字幕，请勿关闭弹窗」）→ **提取完成后直接向量化（无需用户二次确认）**。 |
| 知识库 — 纯视频 | 仅单独上传；系统自动提取视频音轨字幕（提取耗时，弹窗内显示进度条并提示「正在提取字幕，请勿关闭弹窗」）→ **提取完成后直接向量化（无需用户二次确认）**。 |
| 知识库 — 问号说明 | 问号 tooltip 三行换行展示：1）支持格式：Word、PDF、图片、音频、视频；2）能力说明：可解析文档内图片、表格数据；3）上传要求：音频、视频需单独上传处理，文件支持批量。 **【0613】上传格式 / 大小 / 时长上限见下方「知识文件格式 / 大小 / 时长」块。** |
| 文件处理状态 | 每个文件显示：上传中 / 解析中 / 切片中 / 向量化中 / 已向量化 / 向量化失败；**「重试」仅对「向量化失败」的文件提供（已向量化成功的不再提供重试），失败仅支持重试、删除（无替换），失败状态下鼠标悬浮显示失败原因**。 |
| 知识库文件上架 / 下架 | 已向量化成功的文件提供「**上架 / 下架**」切换并展示状态：**仅「上架」文件参与用户问答检索，「下架」文件不参与检索**（与 KP 维度发布状态叠加——KP 已发布且文件已上架方可被检索）。 **【0610】向量化失败的文件检索状态直接显示「下架」（而非空）；失败原因悬浮提示统一为指标同款问号 icon。** |
| 文件名重复规则 | 同一 KP 内上传的知识文件**文件名允许重复、不做唯一性限制**。 |
| 向量化时机 | 文件上传即向量化（与发布解耦）；KP「已发布」前置条件为至少有一份文件「已向量化」，否则发布按钮置灰并提示原因。 |
| 永享 Tab | 列出该 KP 全部图 / 音 / 视条目，支持运营按条目设定永享单价（用户购买后永久解锁该单条内容）；顶部筛选区：按内容名称模糊匹配、按格式单选（图 / 音 / 视）、按价格状态单选（已设价 / 未设价）。 |
| 定价与权益 Tab | 基础权益标签：免费 / 会员（二选一互斥）；界面不常驻显示「二选一互斥」字样，在标签旁放小问号 ⓘ，鼠标悬浮展示互斥说明。 **【0614】免费 / 会员 / 永享说明文案详化避免歧义：免费＝「平台所有用户（无论免费或会员）均可查看此 KP 的非影响内容，含文 / 图 / 音 / 视」；会员＝「仅会员可查看此 KP 的图 / 音 / 视媒体资源，文字内容不受会员身份限制」；并说明「永享标内容需单独计价购买」。** |
| 二维码 Tab | 以「二维码包」为单位的列表（一次创建 = 一个包）。列表字段顺序：二维码包名称、权益模式、**生成的二维码数量、扫描总量、首扫数量、后扫数量**（后四个数量字段均支持排序，无值以「-」表示）、操作（查看 / 下载整包全部二维码 zip）；支持按包名称模糊搜索、按权益模式筛选。创建二维码包填写：包名称（运营手动填写）+ 权益模式（无权益 / 首扫绑定，后扫引导）+ 生成数量——无权益时数量锁定为 1 且不可编辑，仅「首扫绑定，后扫引导」可填写数量。点击「查看」**在页面右侧弹出抽屉 / 进入详情**，按列表展示该包全部二维码（**分页每页 10 条**），字段：二维码 ID、二维码缩略图、首扫状态、首扫时间、首扫账户名称、首扫手机号、后扫数量；筛选支持按**首扫账户名称、首扫手机号**模糊匹配。下载 zip 将整包二维码图片打包，**每张图片以二维码 ID 命名；二维码 ID 全平台唯一且具可读性（非随机乱码字符）**。 |
| 分享 Tab | 「新建分享」为独立弹窗（与下方已分享链接列表分开）；弹窗内设：分享模式（实时同步 / 独立快照）、链接时效 1–15 天（默认 3 天）、访问人数 1–10 人或不限制（默认 1 人，限 N 人时被正确消费 N 次后即作废即使未到期）。已分享链接管理列表字段：链接 / 密码（列内含复制小 icon，点击提示「复制成功」并将链接 + 密码写入剪切板）、模式、时效、消费 / 上限、状态、操作（取消分享）；支持取消分享（取消后链接立即失效，但取消前已被正确消费的链接无法撤回）；放问号 icon 说明取消分享逻辑。 **【0613】已分享列表「时效」列改名为「链接分享有效期」；新建弹窗内有效期字段同步该叫法。** |

> **【0613 知识文件格式 / 大小 / 时长上限】**（配合「去切片方式、直接上传」）
>
> | 类型 | 支持格式 | 单文件大小 | 时长 |
> |---|---|---|---|
> | 文档 | PDF、DOC/DOCX、TXT、MD | ≤ 100 MB | — |
> | 图片 | JPG/JPEG、PNG | ≤ 20 MB | — |
> | 音频 | MP3、WAV、M4A | ≤ 300 MB | ≤ 120 分钟 |
> | 视频 | MP4、MOV | ≤ 1 GB | ≤ 60 分钟（更长建议分段上传） |
>
> 超限按具体原因提示（如「视频时长超过 60 分钟上限，请压缩或分段后重传」）。表格类（XLS/XLSX/CSV）与 PPT/PPTX 暂不收录，如需再加。**【0613-2】文档收敛为 PDF/DOC/DOCX/TXT/MD（去 PPT/PPTX），视频时长上限改 60 分钟；上述格式 / 大小 / 时长须在上传弹窗以文案清晰告知用户。** 数值可按实际算力调整。

### 4. KP 跨机构分享

| 功能点 | 功能描述 |
|---|---|
| 导入分享 KP 入口 | KP 列表顶部「导入分享 KP」按钮，弹窗输入分享链接 + 密码；实时同步导入后直接在列表显示，独立快照导入后 KP 名称自动加「-快照」后缀；独立快照旁加问号说明：「独立快照为分享者发起分享那一刻的内容快照，非接收时刻」。 |
| 共享类型字段 | 每个 KP 标识「自建」或「共享」（被分享或接收他人分享的均标记共享），该字段在 KP 列表卡片呈现。 |
| 实时同步 KP 编辑限制 | 实时同步模式下接收方对该 KP 全部信息只可查看不可编辑（含基础信息、知识库、永享、定价与权益、Agent 绑定、二维码 Tab 等）；该 KP 在接收方机构可正常用于检索问答；接收方不可再次分享；分享仅 KP 创建机构账户可操作。 |

### 5. 运营中心

| 功能点 | 功能描述 |
|---|---|
| 兑换码批量生成与管理 | 权益仅支持会员；输入数量批量生成；权益有效期 1–12 个月（兑换时刻起算）；兑换码全平台唯一、尽量简短；支持导出 Excel；列表字段：兑换码、权益、有效期、状态、兑换时间、兑换用户（微信昵称 / 微信号）、手机号（仅手机号信息不全面，故并列展示）、权益到期时间。 **【0610】点「详情」改为当前页右侧抽屉展示（交互对齐二维码码包）：支持状态单选筛选、兑换用户 / 手机号模糊匹配、导出；明细列「状态」「权益」支持排序。** |
| 订单管理 | 三类订单（会员 / 永享 / 兑换码）筛选与导出；不做退款工单。 **【0613】改为支持退款**：订单列表「操作」列新增「退款」按钮（朝霞橙危险色），支持部分退款；详见下方「订单退款 · 详细规则」块。 **【0614】列表「订单状态 / 退款状态 / 支付方式 / 类型」四字段支持表头排序。** |

> **【0613 订单退款 · 详细规则】**
>
> - **入口**：订单列表「操作」列新增「退款」按钮（朝霞橙危险色，与「详情」区分）；仅「已支付 / 已核销」且未全额退款的订单可退。
> - **退款弹窗**：显示订单实付金额，运营输入本次退款金额（≤ 可退余额，**支持部分退款**）→ 点确认 → **二次确认**「确认退款？资金将原路退回」（危险色，复用 ConfirmDialog）。
> - **资金**：原路退回原支付账户；**生产环境对接微信商户「退款 API」**（所需商户号 / APIv3 密钥 / 商户证书在平台后台「机构 - 微信配置」维护），原型为完整流程仿真。
> - **订单状态扩展**：在「已支付 / 已完成」之后新增 **退款中 → 已退款（全额）/ 部分退款**。
> - **退款时间线**（订单详情内，按时序）：发起时间 · 发起人 → 退款金额 → 处理状态 → 退款成功时间 → 退回账户。
> - **GMV 处理**：累计与区间 GMV 均扣减已成功退款金额；新增**退款指标**（退款笔数、退款金额、退款率）进入主控台**与数据看板**。
>
> **【0613-2 退款精修】**
> - **退款状态独立成列**：与「订单状态」解耦——订单状态保持「已支付 / 已完成」，另起「退款状态」列：未退款 / 退款中 / 部分退款 / 全额退款（更符合电商习惯、干净清晰）。
> - **金额校验（须测试）**：退款金额仅可输入数字，且不得超过订单实付（可退余额）。
> - **二次确认加账户信息**：「将向订单 X · 账户 XX，退款 ¥Y，资金原路退回、操作不可撤销」。 **【0614】二次确认弹窗字段改为垂直并列：发起人：admin（张三）/ 退款对象：昵称（手机号，为空则不显此括号）/ 订单号 / 退款金额 / 资金路径：原路退回客户原支付账户 / 备注：操作一经确认无法撤销。退款提示框（支持部分退款 · 资金原路返回）改为浅灰底圆角引用块 + 珊瑚亮色字 + 无序圆点垂直展开。**
> - **发起人 = 当前登录机构后台账户名 + 真实姓名（括号）**，如「zhangsan（张三）」。
> - **退款时间线改降序**（最新在上）：退款成功 → 退款中 → 运营发起退款，自上而下。
> - 退款提示文案用后台标准字号 + 珊瑚红 + 黄色警告 icon 警示运营。

### 6. 数据看板

> 整个看板顶部统一时间区间筛选（今日 / 7 日 / 30 日 / 自定义），全 Tab 与图表联动；整页支持导出。详细指标见文末「数据看板指标表」。 **【0610】「总览 Tab」区分「实时总览」（累计注册 / 当前会员 / 累计 GMV / 当日 DAU 等存量·累计指标，不随时间筛选）与「经营分析」（随时间区间联动的趋势指标），复用主控台两段式。** **【0614】指标体系整体重划：去掉「总览」Tab（总览职责交主控台），数据看板专做分主题深钻，Tab 重组为 用户分析 / 提问分析 / 营收分析 / 热门 KP 四个域（钱归钱、用户归用户、提问归提问、KP 归 KP）；所有非实时指标一律按今日 / 7 日 / 30 日真联动（含原写死的付费用户 / 付费转化率 / ARPPU、热门 KP 榜单、关键词云数量）。详见文末重写的「数据看板指标表」。**

| 功能点 | 功能描述 |
|---|---|
| ~~总览 Tab~~（**【0614】移除**） | 总览职责交主控台（关键指标驾驶舱），数据看板不再设总览 Tab，避免与主控台重复；原「提问量趋势」图移入「提问分析」Tab。 |
| 用户分析 Tab | DAU/WAU/MAU、新增用户数、次日 /7 日 /30 日留存率、来源分布（扫码 vs 直接访问）、活跃时段分布。 **【0613】新增「地区分布」「性别分布」统计。** **【0614】归类为"用户域"：DAU/WAU/MAU（固定窗口快照，tooltip 注明窗口）、新增用户（区间联动）；留存率 / 来源分布 / 地区分布 / 性别分布（均区间联动）。** |
| 提问分析 Tab | 总提问数、人均提问数、平均会话轮次、受限内容触发率、按 Agent 提问分布。 **【0613】新增「提问领域分布」（按 KP / 领域聚合，如脑科学 / 心理 / 卒中 / 超声…），识别用户最关心的领域。** **【0613-2】「按 Agent 提问分布」与「提问领域分布」改为**左右并列**（数据较多）；再新增「关键词云」（高频提问关键词，词频越高字号越大），做运营 / 商业化分析。** **【0614】归类为"提问域"：① 新增「答案点赞率 / 答案反馈率」摘要 KPI（明细仍在「数据中心·答案反馈」）；② 原「受限内容触发率」移出本 Tab → 转入「营收分析」作付费墙漏斗入口；③「提问量趋势」图自原总览 Tab 移入此处；④ 关键词云升级 UI，并支持 hover 显示当前所选区间下该词的提问数量。** |
| 转化分析 Tab | 付费用户数 / 付费转化率、会员漏斗、永享转化率、ARPPU、续费率。 **【0613】会员漏斗、永享转化漏斗改为「百分比」呈现（各环节相对转化 %，替代纯数量）。** **【0614】改名「营收分析」（钱归钱），重排为三个清晰小节：① 收入与转化（区间 GMV / 付费用户 / 付费转化率 / ARPPU / 续费率，全部改为随区间联动，修复原写死不动）；② 退款（退款金额 / 退款率 / 退款订单数 / 净 GMV）；③ 转化漏斗（受限内容触发率 → 会员漏斗 · 永享转化，**独立小节标题**，消除"漏斗被误读为退款下属"）。** |
| 热门 KP Tab | 提问数 TOP 10 KP、付费转化贡献 TOP 10 KP、永享购买 TOP 10 KP。 **【0613】「提问数 TOP 10」改名为「被提问数 TOP 10 KP」（语义更清晰）。** **【0614】三个 TOP10 榜单改为随今日 / 7 日 / 30 日区间联动（不同区间给不同榜单数据）。** |
| 答案反馈 Tab（新增） | **【0613】新增。** 汇总前台答案点赞 / 点踩 / 反馈数据（前台已埋点、后台原未呈现）：点赞率、点踩率 + 反馈明细列表（问题、反馈标签如「有害 / 不安全」「虚假信息」「没有帮助」「其他」、提交时间、关联 Agent / KP），供运营定位问题、迭代知识库。 **【0613-2】「答案反馈」从数据看板 Tab 提升为「数据中心」下与「数据看板」同级的**独立菜单**（运营工作台）：去掉「待处理反馈」指标；明细改**分页列表** + 按反馈标签 / 时间区间 / 问题搜索；去「关联 Agent/KP」列，新增「反馈人（标注是否会员）」；点「详情」查看用户问题 + AI 答案（含当时推送的图 / 音 / 视多模态知识）；支持**导出**（仅问题 + 答案文本，不含媒体资源与溯源）。** **【0614】列表「反馈标签」列支持排序；「反馈人」直接显示昵称 + 是否会员标识（去「微信用户」字样）；删除列表与详情中的「关联 Agent / KP」列 / 区块；详情弹窗「用户问题 / AI 答案」改为**左右气泡布局**（仿前台 AI 对话）。** **【0614c】机构 / 平台后台答案反馈详情抽为**公共组件**并进一步对齐前台：用户问题=主题色渐变气泡、AI 答案=白底+边框气泡；「当时推送的多模态知识」**改名「相关媒体资源」**并改**横向缩略图条**（左右滑动、点击放大）；反馈标签 / 反馈人 / 日期 与问答气泡间加**浅灰分隔线**（避免误认成 C 端会话界面）；弹窗增加底部留白。** **【0614c】修复：相关媒体资源缩略图鼠标悬浮放大时顶角被滚动容器裁剪（`overflow-x:auto` 令 `overflow-y` 计算为 `auto`）——给缩略图条上下留白 + 悬浮改 scale + 抬层级，放大后完整可见。** |

### 7. 客服配置

| 功能点 | 功能描述 |
|---|---|
| 客服配置 | 上传一个客服二维码（企微 / 个微），填写电话、邮箱（均非必填）；直接驱动前台「联系客服」弹窗渲染。 |

### 8. C 端用户管理

| 功能点 | 功能描述 |
|---|---|
| C 端用户列表 | 本机构 C 端用户列表 + 权益概览（会员状态 + 已购永享数）；不做客服补发工单。字段名为「**用户昵称**」。**昵称规则**：微信登录用户取微信昵称；手机号登录注册用户昵称取「用户 + 手机号后四位」（如 139****0000 → 用户0000，可重复）。会员状态、已购永享、累计 GMV、最新登录时间均支持排序。列表带分页控件。 **【0613】列表增加「地区」「性别」列（微信授权带回，无则留空）；增加「导出」（导出当前筛选结果）。** **【0613-2】列表支持按「地区」「性别」排序；用户详情「全部订单」列表支持按「类型」「状态」「支付方式」排序。** |
| C 端用户详情 | 点击进入详情：手机号、微信号、活跃度、会员状态、已购永享、全部订单。**已购永享放大预览按后台 UI 规范设计**（居中白窗，非全屏黑底），视频支持倍速播放（0.5x/1x/1.5x/2x）。订单详情按类型差异化同前台（会员含续费方式、永享整行可点预览、兑换码极简「详情」）。 **【0613】详情展示地区、性别（无则空）。** **【0614】「全部订单」内嵌列表增加「退款状态」列、规范「订单状态」字段，字段与「订单管理」列表对齐并尽量复用同一列定义，避免歧义。** |

### 9. 系统配置

| 功能点 | 功能描述 |
|---|---|
| AI 会员价格设置 | 配置首月折扣价、月度价、续费规则（机构维度），直接驱动前台会员订阅页展示。 |
| 回答策略配置 | 配置「知识库未检索到相关知识」时的回答兜底策略，三选一：① 直接由大模型回答；② 直接告知「知识库暂无此资料」；③ 由大模型回答但在答案中标注「此为大模型生成、非知识库检索」。 **【0613】保留 3 条并明确语义（你想加的「直接回答、不告知来源」即策略①，无需新增第 4 条）：① 严谨模式—仅用知识库，未命中答「暂无相关资料」；② 透明兜底（默认）—未命中由大模型补充并标注「此为大模型生成」；③ 流畅模式—未命中由大模型直接补充、不额外标注。** |

---

## 三、平台超管 Web PC（平台超级管理员）

> **【0614c 菜单重组】** 平台后台侧边栏按**机构后台四大板块镜像**重排，平台 ↔ 机构心智对称、层次清晰（原「平台数据中心」一坨 6 项已拆开）：
> - **机构管理**：机构管理（机构列表页，**菜单名由「机构列表」改为「机构管理」**）、机构账户、**角色权限**（角色是分配给机构账户的，从原「全局策略」移入此处、与机构账户同级）
> - **产品中心**（镜像机构后台·产品中心）：**全域知识产品 KP**（原「全域知识 KP」，与机构后台「知识产品 KP」对齐）、全域 Agent 人设
> - **运营中心**（镜像机构后台·运营中心）：全域用户、全域订单
> - **数据中心**（镜像机构后台·数据中心）：全域答案反馈、**全域模型用量**（原「模型用量」更名，统一「全域」前缀）
> - **系统设置**（镜像机构后台·系统设置）：默认 LLM（平台级模型配置；模型「用量」属数据、归数据中心，模型「配置」属设置、归此处）
> - 组名沿用机构后台同名，靠各项「全域」前缀标明跨机构只读监管范围。

### 1. 登录与主控台

| 功能点 | 功能描述 |
|---|---|
| 登录页 | 平台超管账号 + 密码登录。 **【0615】登录页与机构后台共用同一组件，图形验证码支持点击刷新「换一张」（随机重新生成 4 位字符）。** |
| 顶栏「入驻清单」下载按钮 | **【0615-2】新增**。平台后台顶栏（角色 / 头像旁）固定「AI问书·新机构入驻微信对接清单」按钮，点击下载机构入驻所需的微信对接清单 Word（面向机构客户：在微信哪些平台做什么、提供哪些资料、微信内 / 外双场景与注意事项）。 |
| 个人中心（顶栏入口） | **【0615】新增。** 顶栏右上原「姓名 · 角色」下拉项改为「个人中心」，点击进入个人中心落地页（保留「退出登录」）。落地页展示：头像、账户名称、姓名、角色、联系方式（手机 / 邮箱）、账户状态（平台超管账户无所属机构 / 上级机构，故不展示该两项）。**仅头像可上传更新**（相册选图 + 圆形裁剪）；其余信息只读不可编辑。**【0615-2】平台超管个人中心不展示「若需更改账户信息，请联系平台管理员」灰字提示（机构后台保留）。** |
| 主控台 Dashboard | 全平台关键 KPI（机构数、累计用户、GMV、提问总量等）+ 趋势图（去掉告警 KPI）。 **【0613】主控台与数据看板增加「全部机构 / 指定机构」筛选器；补充用户相关统计（地区 / 性别分布、活跃等）。** **【0614】实时总览增加「净 GMV（扣退款）」关键指标（tooltip 注明退款额 · 率）；主控台保持精简（生态 + 规模 + 钱），用户画像深钻走「全域用户」，不塞进主控台。** |

### 2. 机构管理（含机构账户管理子菜单）

| 功能点 | 功能描述 |
|---|---|
| 机构列表 | 字段：机构 ID、机构名称、状态（正常 / 停用）、上级机构（有则展示名称，无则 —）、LLM 配置（平台默认 / 自配厂商名称）、微信支付（未配置 / 已配置）、操作（详情 / 停用·恢复）。 **【0614c】菜单 / 页面更名为「机构管理」；**删去「序号」列**（机构 ID 已可定位）；列表新增 **套餐** 列（基础 / 专业 / 旗舰 / 定制，**可按档位排序 + 按套餐筛选**）+ **KP 用量 / Token 额度 / 存储空间** 三列，均以「**已用 / 上限**」展示，已用值颜色规则与用量看板一致（见下方配额块）、上限由套餐推断、定制版单列；让平台一眼看清各机构套餐与配额消耗。** **【0615-6】**去掉列表「已用」值按使用率变色**（原 < 70% 黑 / 70~90% 橙 / ≥ 90% 珊瑚红 + 加粗）——整列颜色太花太乱，统一改为普通文字（已用黑字、「/ 上限」灰字）。用量预警仍由机构详情用量看板的进度条体现。** |
| 上级机构关系（数据模型） | 机构表自带可空 `parentId`（自引用），`null` = 顶级机构；不引入独立「集团 / 类型」实体——上级机构即「碰巧有下级的普通机构」。**V1 约束：只允许一层（集团→分社）**：① 可被选为上级的，仅限自身 `parentId` 为空的顶级机构；② 已有下级的机构不可再设上级；③ 不可选自己为上级。由此杜绝三层套娃与自环。 |
| 机构状态联动 | 正常：可正常登录后台、访问前台；停用：访问前后台均提示「机构服务已暂停，如需恢复请联系管理员」。 |
| 机构状态变更入口 | 变更入口在**机构列表的操作列**（停用 / 恢复，点击弹出二次确认；停用为危险操作，确认按钮用警示色）；机构详情页的状态**仅展示、不可在此操作**，并提示「状态变更请在机构列表进行」。 |
| 创建机构弹窗 | 弹窗字段：机构名称、上级机构（选填，下拉**仅列出顶级机构**，建立层级关系，账户据此自动推断）、备注（选填）、状态（默认正常）、LLM 配置（平台默认 / 自配）。机构 ID 在创建后由系统自动生成、弹窗内不展示；不再有「出版方」字段（已统一为上级机构）。 **【0613】创建机构时增加配额：KP 数量上限、存储空间上限、月度 Token 额度；提供 4 档套餐预设一键填充，详见下方「机构配额与套餐」块。** **【0613-2】修正：配额 / 套餐**不在创建机构弹窗配置**，移至机构详情的独立「套餐 / 配额」Tab（创建后再配）。** **【0614】(三1) 创建机构说明文案精简为「仅顶级机构可被选为上级」（删冗余句）；(三5) 创建机构弹窗增加「联系人手机号 + 联系人姓名」字段（机构联系人，手机号**可重复**、姓名如张三李四）。** |
| 机构详情 Tab 1 — 基本资料 | 基础信息查看与编辑（含上级机构）。 **【0614】基本资料增加「联系人手机号 + 联系人姓名」字段（与创建机构一致，手机号可重复，用于配额阈值预警短信通知）。** |
| 机构详情 Tab 2 — 机构配置 | 左侧二级 Tab 自上而下：LLM 配置 / 联网配置 / 微信支付配置。 **【0613】「微信支付配置」升级为「微信配置」，含两块：① 微信公众号（AppID、AppSecret、网页授权回调域名、JS 安全域名——用于微信登录 / 网页授权获取头像昵称性别地区）② 微信支付（商户号 MchID、APIv3 密钥、商户证书——用于支付 / 退款 / 自动续费），并标注公众号 AppID 需与商户号绑定。二级 Tab 变为：LLM 配置 / 联网配置 / 微信配置。** **【0613-2】表单优化：公众号 / 支付两块用**分区卡片**强化视觉区分；底部提示改后台标准字号 / 色；新增**结构化限制提示**——网页授权获取用户资料需「已微信认证的服务号」（订阅号 / 个人号不支持）；JSAPI 支付需认证服务号 + 商户号且公众号 AppID 与商户号绑定（同主体或授权）。** **【0614】(三4) 公众号 / 支付的用途说明改为**行内灰色小号字**（不再单独大字一行）；公众号、支付均为**必填**，字段加红色 ＊，文案中两处「选填」改「必填」。** |
| 机构详情 Tab 3 — 用量看板 | 平台视角单机构运营健康度看板，支持时间区间筛选 + 导出。详细指标见文末「用量看板指标表」。 **【0613】增加「配额用量」：KP 数、存储、本周期 Token 的「已用 / 上限」进度条，接近上限预警。** **【0614】(三5) 商业化区在累计 GMV / 当前会员下增加「退款金额 + 退款率」两指标；配额用量"接近上限"演示：达 70% / 80% / 90% / 95% 标注已触发阈值并示意「已向机构联系人发送预警短信」；Token 额度 / 已用统一「亿」单位。配额超限的 C 端 / 机构后台提示见「机构配额与套餐预设」块【0614】补充。** **【0615-6】顶部配额展示升级为**当前生效订阅卡**（与订阅 / 配额 Tab、机构后台主控台同款共享组件）：套餐 / 状态 / 加油包数 / 有效期 / 剩余天数 + 三项「已用 / 上限（含加油包）」进度条；**不显示商务负责人、不显示「新建订阅」按钮**。** |
| 机构详情 Tab — 订阅 / 配额（原「套餐 / 配额」） | **【0615】改为订阅闭环**：顶部「当前生效订阅」卡（套餐 + 额度「已用 / 上限」进度条 + 有效期 + 倒计时）；下方「订阅记录」+「新建订阅」。 **【0615-2】订阅类型只分两种——**订阅**（常规，定套餐 + 有效期；新签 / 续签仅触发时机先后、不手选）、**加油包**（期中加量，**即时生效、额度累加**到机构当前，不改套餐）；每笔有**生效 / 未生效状态开关**（默认新建即生效），当前可用额度 = 生效订阅 + 生效加油包累加。订阅记录列：订阅号（自动生成）/ 类型 / 套餐 / 额度（已用 / 上限）/ 有效期 / 商务负责人 / 状态 / 创建时间（年月日时分秒）/ 创建人 / 操作（**详情** + **复制**）；支持按类型 / 套餐 / 商务负责人 / 状态筛选；「详情」弹窗含备注，「复制」把该订阅数据带入新建表单便捷填写。新建弹窗：套餐竖排左对齐选择、选套餐带出额度可手调、商务负责人 / 备注选填，无合同金额。** **【0615-3】订阅记录改**按订阅维度展示**（仅订阅、不混加油包）：同一机构同时只有一个「生效」订阅，可预建未生效下一期；每行显示该订阅的**加油包数**，操作列「加油包」按钮 → **右抽屉**展示该订阅的加油包列表（额度已用 / 上限）+「新建加油包」（即时生效、额度累加、跟随订阅到期日）。加油包额度展示去掉「加量」字样、显已用 / 上限。** **【0615-5】精修：(1) 新建订阅弹窗底部说明块与上方「状态开关 / 输入框」**左对齐**（缩进至控件列）；(2) 订阅记录「额度（已用 / 上限）」列改为「**总额度（已用 / 上限，含加油包）**」——数值 = 该订阅基础额度 + 其名下生效加油包累加（与顶部当前生效订阅卡口径一致）；列表默认**按创建时间倒序**；(3) 加油包右抽屉重构：「**新建加油包**」按钮移到抽屉**上方**（原在底部太靠下）；加油包改**列表展示**（参考兑换码详情页：创建时间 / 创建人 / 加油包额度 / 备注 / 状态，**去掉商务负责人**）；空数据走标准空态（不再是裸文字）；新建表单 KP / 存储 / Token 三项放**同一行**，**三项全为 0 不可创建**并提示「请至少为一项加量额度填写大于 0 的值」；抽屉整体按设计规范重做美化。** **【0615-6】(1) **新建订阅生效规则定稿（方案 A+C）**：① 同一时间段只能有一个生效订阅；② 有生效订阅时**最多再预建 1 期未生效订阅**（续约）；③ 新建有效期**不可与该机构已有订阅重叠**（重叠则拦截、不可创建）；④ 未生效订阅**到生效日期自动转生效**、无需手动。故**去掉「状态」手动开关**，状态由有效期自动判定（起始日 > 今天 = 未生效；已覆盖今天 = 生效）；有效期默认从最晚订阅到期次日衔接（天然不重叠）。以上规则以**无序列表**写进新建订阅说明块、**不换行依次展示**。(2) 加油包列表「加油包额度」列**只显该加油包的额度**（如 `0.5 亿 Token`），不再显「已用 / 上限」。(3) 加油包空态插画换为更贴合 AI 问书品牌的图形（去掉星星圆圈、奶白底 + 电光靛 / 朝霞橙）。(4) **当前生效订阅卡抽为共享组件 CurrentSubCard**，机构详情订阅 Tab（含商务负责人 + 新建按钮）/ 机构详情用量看板 + 机构后台主控台（均不含商务负责人 / 新建按钮）三处复用。** **【0615-7】(1) 订阅状态改**三态、由有效期自动判定**：未生效（今天 < 生效日）/ 生效（生效日 ≤ 今天 ≤ 到期日）/ **已过期**（今天 > 到期日）；不再手动设状态。(2) **误建删除**：订阅订单操作列对「**零用量 + 无加油包 + 非已过期**」的订单显「删除」（危险色 + 二次确认、删除不可恢复）；生效中有消耗 / 已过期**不可删**，保留服务与计费历史——这样既能处理"机构第一笔订阅（生效态）建错了"（刚建、还没消耗即可删），又不丢历史。(3) **改名**：「订阅记录」→「订阅订单」，字段「订阅号」→「订阅订单 ID」。** |
| 订阅订单（全域菜单 · 平台超管） | **【0615】新增**，机构管理组下「订阅订单」全域页：跨机构汇总所有订阅 / 加油包。 **【0615-2】顶部汇总改 KPI 卡（与主控台风格一致）：订阅总数 / 生效 / 未生效 / 加油包 / 90 天内到期；列含 订阅号 / 机构 / 类型 / 套餐 / 额度（已用 / 上限）/ 有效期 / 商务负责人 / 状态 / 创建时间 / 查看机构；搜索按机构 / 商务负责人模糊匹配，另支持机构 / 类型 / 套餐 / 状态筛选。B 端订阅订单（机构 → 平台）与 C 端全域订单（终端用户 → 机构）两套体系，互不混淆。** **【0615-5】精修：(1) 列表**只展示「订阅」、不再混入加油包**（去掉类型列与类型筛选）；额度列同步改「**总额度（已用 / 上限，含加油包）**」；新增「**加油包**」数量列。(2) 操作列新增「**加油包**」按钮 → **右抽屉**展示该订阅订单的加油包列表，并支持在抽屉内**直接为其新建加油包**（与机构详情订阅记录同一组件、同一交互）。(3) 顶部 KPI 指标每个名称后加**说明 icon，鼠标悬浮显示计算规则面板**（同主控台交互）；订阅总数 / 生效 / 未生效仅计「订阅」，加油包单列计数。** **【0615-7】同步：状态三态由有效期自动判定（含**已过期**），状态筛选增加「已过期」；字段「订阅号」→「订阅订单 ID」；生效 / 未生效 KPI 计数改按自动判定口径。** |
| 机构品牌外观（新增 Tab / 区块） | **【0613】新增。** Organization 增加 logoUrl + 主视觉色 + 辅助视觉色（主→辅**渐变**）。机构创建 / 详情「品牌外观」配置：上传机构 logo → 智能取色填充主 / 辅色（可手调）→ 渐变预览。前台品牌球、主强调色与机构后台主色读取该机构配色，实现每机构差异化主题。 **【0613-2】「智能取色」在原型中真实实现：上传 logo 后用 canvas 提取主 / 辅色填充并实时更新渐变预览；界面去掉「原型先落…全量主题化作后续」说明文案。** **【0614】(三6) 主 / 辅视觉色块由矩形改**圆角**；渐变预览整体**缩小**（球 / 条 / 矩形示意收紧），不再过大。** |
| 机构账户管理（子菜单） | 置于「机构管理」菜单下。全平台账户列表字段：序号、账户 ID、账户名称、姓名、所属机构、上级机构、角色、联系方式、状态（正常 / 停用）、最近登录时间、创建时间、操作。 **【0610】「上级机构」「角色」「状态」三列支持点击表头排序。** |
| 平台账户（系统设置组） | **【0615-2】新增**。平台方人员账户（平台超级管理员 / 平台运营 / 平台财务），**平台级角色、无所属机构**，与「机构账户」明显区分：机构账户在「机构管理」组、归属某机构；平台账户在「系统设置」组、独立列表与创建。列表：账户名称 / 姓名 / 角色 / 联系方式 / 状态 / 创建时间 / 操作（重置密码 · 停用 / 启用）；「新建平台账户」弹窗填账户名称 / 姓名 / 角色 / 手机号（密码由系统生成、不在界面设置）。 **【0615-3】创建 / 重置密码后弹**账户凭证弹窗**（账号 / 密码 / 姓名 / 机构 / 角色 + 一键复制，密码仅此一次明文）；停用 / 启用成功弹 3 秒提示。机构账户同此交互（操作列加重置密码、创建 / 重置弹同凭证弹窗）。** |
| 账户新建 | 弹窗字段：账户名称、姓名、所属机构（必选，上级机构由所选机构自动推断，无需单独选择）、角色（创建时即可指定，后期可变更）、联系方式、备注（选填）、状态（默认正常）。 |
| 账户状态联动 | 正常：可访问机构后台；停用：访问后台提示「账户服务已暂停，如需恢复请联系管理员」。 |

> **【0613 机构配额与套餐预设（商务定价参考）】**
>
> Organization 新增三个限额字段：**KP 数量上限、存储空间上限（GB）、月度 Token 额度**。KP 数 / 存储为累计上限；Token 额度按计费周期（月）重置；机构可设服务有效期（起止时间）。创建机构弹窗与机构详情可选「套餐」一键填充，亦可手动微调；机构用量看板展示「已用 / 上限」进度并在接近上限时预警。 **【0615 / 0615-2】配额改由「订阅订单」承载**：机构当前可用额度 = 当前生效「订阅」+ 生效「加油包」累加；常规续约新建「订阅」（定套餐 + 有效期），期中加量新建「加油包」（即时生效、额度累加、不改套餐）；每笔有生效 / 未生效开关，不再独立手填覆盖配额。详见上方「机构详情 Tab — 订阅 / 配额」与「订阅订单（全域菜单）」两行。
>
> **【0614c 配额口径明确】** KP 数量、存储空间、月度 Token 额度三项配额**均为机构「服务有效期内」的额度上限**（机构详情配额表单与机构管理列表均按此口径）。机构详情「月度 Token 额度」字段后文案由「按计费周期·月重置」改为「单位：亿（服务有效期内）」。
>
> **【0614c 配额「已用 / 上限」颜色规则】** 两处共用同一套**预警阈值与预警色**（70% 预警、90% 危险；橙 `--amber`、珊瑚红 `--terra`），仅「正常态」表现不同：
> - **机构详情 · 用量看板（进度条）**：进度条**始终着色**——< 70% 青 `--jade` / 70%~90% 橙 / ≥ 90% 珊瑚红。
> - **机构管理列表（KP 用量 / Token 额度 / 存储空间三列的已用值）**：**正常态（< 70%）用黑字 `--ink`**（避免整列花花绿绿），**仅预警时变色并加粗**——70%~90% 橙、≥ 90% 珊瑚红。
>
> **【0614c 时间面板统一】**「服务有效期」改用与主控台一致的**日历面板**选「开始 ~ 截止」（**仅到年月日**，不需时分秒），替换原生 datetime-local（视觉不符主题）。**两个后台所有时间面板统一为该日历样式**，并支持**点中间「2026 年 6 月」标题下钻**：日 → 点标题进月选择 → 再点进年选择，可灵活跳任意年 / 月再选日（不再只能逐月翻）。
>
> | 套餐 | KP 数 | 存储 | 月度 Token |
> |---|---|---|---|
> | 基础版 | 10 | 20 GB | 5,000 万 |
> | 专业版 | 50 | 100 GB | 2 亿 |
> | 旗舰版 | 200 | 500 GB | 10 亿 |
> | 定制版 | 自定义 / 不限量 | 自定义 | 自定义 |
>
> 数值为定价起点，按商务策略调整。
>
> **【0613-2】** 配置入口移至**机构详情独立「套餐 / 配额」Tab**（不在创建机构弹窗）；套餐改**平铺卡片**（基础 / 专业 / 旗舰，点选自动填充、仍可手改）而非下拉；单位：**KP = 个、存储 = GB、月度 Token = 百万(M) / 亿**（与行业「每千 / 每百万 Tokens」计价一致，弃用「万」）。
>
> **【0614】**
> - **(三3) Token 额度单位统一为「亿」**：基础 0.5 亿 / 专业 2 亿 / 旗舰 10 亿；套餐卡、配额表单、机构用量看板全部统一「亿」，不再中英混用（M）。**【0614c】配额表单「月度 Token 额度」输入框只填数字（如 `2`），单位「亿」作后缀 / 单位标——与 KP「个」、存储「GB」一致，不再把「亿」塞进输入值（套餐卡同步显示为「月度 Token 2 亿」）。**
> - **(三2) 套餐与配额歧义解决**：机构保留套餐标签，但选套餐填充后**一旦手动改动任一配额值，套餐标签自动变为「自定义套餐」**并以 badge 明示——既保留商务标签，又消除"选了基础版却把配额改成别的数"的歧义。
> - **(三5) 配额超限闭环（跨端）**：机构后台创建 KP 超 KP 数上限 / 上传文件超存储上限 → 提示并阻断（如「已达 KP 数量上限，请联系平台扩容」）；前台 C 端当机构本月 Token 超限 → **友好提示**「当前机构 Token 已达上限，请联系客服」；达 70% / 80% / 90% / 95% 给机构联系人发短信预警（原型以可视化演示，见用量看板）。**【0614c】前台 Token 超额拦截改进：拦截任何提问 / 语音 / 实时入口、问题不发出；**不再弹 3 秒 toast**，改为让顶部已有的珊瑚红提示条**放大抖动 + 跳亮一下**，引导用户看已有提示。**
>
> **【0614b】**
> - **(三3) 用量看板「Token 预警短信」出现时机（明确规则）**：用量看板顶部「本月 Token 已用 XX%」预警条**并非常驻**——仅当本计费周期 Token 已用率 **≥ 70%** 时出现并常驻；**< 70% 不显示**。短信推送按阈值**首次达到时各推一次**（70% / 80% / 90% / 95% 四档，同一计费周期内同一阈值不重复推送，下一周期重置归零）。原型以 88% 态可视化演示「已发 70% / 80% 预警短信，待发 90% / 95%」。
> - **(三6) 手机号唯一性规则（明确规则）**：**C 端用户手机号「机构内唯一、全平台可重复」**——同一手机号在**同一机构内唯一、不可重复**（作该机构内登录凭证），但**可在不同机构各注册一个账户**（如张三在 A、B 两机构各有账户、用同一手机号，允许）；**机构联系人手机号、机构账户联系人手机号均可重复**（仅作配额预警通知 / 联系用途，可与其他机构 / 账户相同，亦可彼此相同）。机构联系人、机构账户联系人为**必填**。三端在上述字段旁以「?」悬浮面板（无序点）说明此差异。**【0614c 更正】此前误写为「C 端手机号全平台唯一」，实为「机构内唯一、全平台可重复」。**

### 3. 角色与权限

| 功能点 | 功能描述 |
|---|---|
| 角色权限重构（机构 / 平台双类型） | **【0615-3】新增**。「角色权限」从「机构管理」组移到「**系统设置**」组；按「**角色类型 = 机构 / 平台**」分别管理——机构角色用于机构账户、平台角色用于平台账户，**两者权限点不同**（机构角色覆盖机构后台功能；平台角色覆盖平台后台机构管理 / 全域 / 系统设置等）。页面顶部「机构角色 / 平台角色」切换；新建角色时选角色类型；权限仍为三态（无 / 只读 / 可操作）。 **【0615-5】复核确认：「角色权限」菜单当前已位于「系统设置」组（线上旧版仍显示在「机构管理」组下，将随本批部署一并更正）。同时把平台角色权限矩阵里的「角色权限」**权限点（p.role）** 从「机构管理」分组移到「系统设置」分组（置于 平台账户 与 默认 LLM 之间），与菜单分组保持一致（菜单一致性）。** |
| 账户密码体系（两后台通用） | **【0615-3】新增**。**密码规则：8–16 位，至少同时包含字母和数字，区分大小写，可含特殊字符，不含空格。** 机构账户 / 平台账户操作列均有「重置密码」，创建账户与重置密码后均弹「账户凭证弹窗」（账号 / 密码 / 姓名 / 机构 / 角色 + 一键复制，密码仅此一次明文）。两后台个人中心支持「修改登录密码」（旧密码 1 次 + 新密码 2 次；旧密码输入项带问号提示「若忘记旧密码可联系平台管理员处理」）。 |
| agent.prompt.edit 权限点 | 控制 Agent「回答 Prompt」是否可编辑；所有内置角色默认不勾选但可见可勾选。 |
| 内置角色超管可改 | 平台超管可对所有内置角色勾选 / 取消勾选权限点。 |
| 权限点三态（无 / 只读 / 可操作） | **【0613】新增模型。** 角色对每个功能点的权限从「勾选 / 不勾选」升级为三态：**无权限 / 仅查看 / 可操作**。角色编辑网格每个功能行用三态选择器替代单一勾选；原「只读角色」不再特殊硬编码，而是「所有功能均设为仅查看」的普通角色。按功能粒度控制读写、更灵活。 **【0613-2】页面加「无 / 只读 / 可操作」**图例说明**：无＝看不见该菜单；只读＝可进入查看、但不能编辑 / 操作（如退款）；可操作＝该功能全部操作可用。** **【0614】(三8) 删除文案「按功能粒度控制读写，替代角色级只读」；三态图例从页面常驻挪到「运营权限」表头右侧问号 icon 的 hover 浮层内，**垂直**展示 无 / 只读 / 可操作（无序圆点连接），问号 icon 与「权限」文字间距适中。** |

### 4. 上级机构账户体验

| 功能点 | 功能描述 |
|---|---|
| 概念替换 | 原「集团 / 出版方」一律改为「上级机构」（含字段名、菜单名、文案）。 |
| 上级机构数据看板（唯一菜单） | 仅给一个「主控台」菜单，内含三块：① 顶部汇总卡（所有关联机构合计：累计用户、当前会员、累计 GMV、累计提问数）；② 关联机构卡片列表（每张卡展示一个机构的 DAU、GMV、会员数、KP 数等关键指标，点击下钻）；③ 跨机构对比（折线 / 柱状图对比关联机构趋势）。 |
| 下钻只读视图 | 点击机构卡片进入该机构只读详情，可看：机构详情、用量看板、KP 列表（只读）、C 端用户（只读）；最大化复用机构详情 / 用量看板既有页面布局、UI 与组件；隐藏账户管理、系统配置等敏感菜单。 |

### 5. 平台数据中心

| 功能点 | 功能描述 |
|---|---|
| 全域用户（跨机构 C 端用户管理） | 筛选：账户名称模糊、微信号模糊、手机号模糊、会员状态下拉、归属机构下拉；列表额外展示微信号字段（应对微信注册手机号可能为空的场景）。 **【0614b】(三7) 用户详情「全部订单」**复用全域订单同一份数据**（按当前用户过滤），不再单写一套；含**退款状态**列，详情可下钻订单详情。** |
| 模型用量（平台默认 LLM 用量看板） | 默认展示平台全部机构近一周数据；支持机构筛选与时间区间筛选，下方图表联动；用更清晰表达多机构趋势的图表（多折线 / 堆叠面积），不用 7 天堆叠柱；去掉告警状态。 **【0610】区分「实时总览」（平台开通至今累计 tokens / 累计调用次数）与「经营分析」（随时间区间的 tokens、调用次数、平均响应、各机构趋势），复用主控台两段式。** **【0613】「全部机构」视图改为「总量趋势（单线 / 面积）+ Top 机构 token 排行榜（条形）」，弃多折线以适配数十至上百机构；选中单机构时显示该机构趋势。** **【0613-2】Token 额度 / 用量统一用**百万(M) / 亿**为单位（弃用「万」，与行业「每千 / 每百万 Tokens」计价一致）；「经营分析」区重排：总量趋势不再占满整宽、与 Top 机构排行更紧凑（区间指标可竖排），提升空间利用率。** **【0614】(三7) 修复联动：经营分析全部指标（区间 tokens / 调用次数 / 平均响应）+ 全平台总量趋势 + Top 机构 token 排行，均按今日 / 近 7 天 / 30 天给不同假数据真联动；删除「当期」硬编码字样，改为跟随所选时间区间显示（如「Top 机构 token 排行 · 近 7 天」）。**通查两后台所有「当期」字样统一改为按所选时间显示。** **【0614b】Token / 调用统一中文万进制（万 / 亿），不再用英文 M / k：累计 1.28亿、区间 860万 token、调用 24万次、Top 机构 640万 等；并显单位（token / 次）。详见文末「指标数值单位规范」。** |
| 全域订单（全平台订单） | 默认按订单创建时间降序；支持筛选：机构、订单创建时间、订单状态、微信号模糊、手机号模糊。 **【0614b】(三8) 列表字段与订单详情**完全复用机构后台订单的列表 / 详情**（同一份数据与退款 store，不再各写一套），仅多一列**归属机构** + 机构筛选；补齐机构后台已有的**退款状态**列（此前缺失），详情含退款信息与退款时间线。** |
| 全域 KP（新增） | **【0613】新增。** 平台视角跨机构知识产品聚合视图（只读）：列出全平台 KP（机构、名称、状态、文件数、提问数等），支持按机构 / 状态 / 名称筛选，供平台监管与统计。**Agent / 用户反馈 / 兑换码不单独建全域视图**——如需查看，从「机构详情」下钻该机构数据（无需登录机构账户或直查数据库）。 **【0614b】改名「全域知识 KP」；列表 → **卡片布局**（复用机构后台 KP 列表视觉），每张卡片标注**归属机构**；保留机构 / 状态 / 名称筛选；平台只读。** **【0614b】(三9 / 三10) 删除卡片上方聚合说明文案；卡片删去「书籍 / 系列 / 专家库」粒度标、「免费 / 会员」、创建时间（只留名称 / 机构 / 状态 / 永享）；**点击卡片进入 KP 详情，直接复用机构后台 KP 详情页**（同一组件，删除后返回全域 KP 列表）。** |
| 全域 Agent 人设（新增） | **【0614b】新增**（与全域知识 KP 同级）。平台视角跨机构 Agent 人设聚合：卡片布局（复用机构后台 Agent 卡片），每卡显示**归属机构** + Agent 名 + 关联 KP 数 + 音色；支持机构 / 名称筛选。 **【0614b】(三11) 点击卡片进入 Agent 详情，**复用机构后台 Agent 详情页**；其中「回答 Prompt」对平台超管**开放编辑**（不受角色权限限制，超管可对全平台任意机构、任意 Agent 的回答 Prompt 修改并保存、即时生效），机构侧仍为只读。** |
| 全域答案反馈（新增） | **【0614b】新增**（与全域 Agent 人设同级）。汇总**全平台各机构** C 端答案点踩 / 反馈，复用机构后台「答案反馈」工作台（分页列表 + 左右气泡详情 + 导出问答文本）；列表与详情多一列 / 行**归属机构**，顶部支持**机构筛选**，便于平台监管与质量分析。 |
| 全域页通用约束 | **【0614b】** 凡平台后台「全域」页（全域用户 / 全域订单 / 全域知识 KP / 全域 Agent 人设 / 全域答案反馈）：**每行（卡）显示归属机构、详情显示归属机构、顶部支持按机构筛选**。 |

### 6. 全局策略

| 功能点 | 功能描述 |
|---|---|
| 默认 LLM 模型配置 | 配置可选模型清单（具体到版本号如 Qwen 3.5 27B），支持新建录入也支持下拉选；不配置限速。**模型名称全平台唯一**（新建 / 编辑时校验重名并阻断保存）。 |

---

## 四、全局交互状态（三端通用）

### 1. 通用状态规范

| 功能点 | 功能描述 |
|---|---|
| 空态 | 列表 / 会话无数据时展示插画 + 说明文案 + 主操作入口（如「+ 新建 KP」「去看看」）；搜索 / 筛选无结果展示「没有匹配的结果，换个条件试试」。覆盖：KP/Agent/订单/兑换码/C端用户/分享链接/二维码包/永享/知识库文件列表、我的永享、我的订单、会话历史。 |
| 加载 / 骨架屏 | 列表 / 卡片 / 表格首次加载用骨架屏占位；AI 答案流式生成中显示打字机光标 +「停止生成」；按钮（提交 / 支付 / 生成 / 批量生成）执行中显示 loading 并禁用重复点击；字幕提取、文件向量化展示进度；数据看板图表加载占位。 |
| 错误 / 重试 | 请求失败展示「加载失败，请重试」+ 重试按钮，引导下一步而非只报错；支付失败页提供「重新支付 / 返回」。 |
| 表单校验 | 输入项即时校验，错误在字段下方红字提示（手机号格式、验证码错误等）；提交前阻断并聚焦首个错误项。 |
| 停用 / 越权 / 失效 | 机构停用：「机构服务已暂停，如需恢复请联系管理员」；账户停用：「账户服务已暂停，如需恢复请联系管理员」；无权限 / 页面不存在引导返回；前台二维码失效弹窗提示 3 秒跳首页。 |
| 二次确认 | 删除、发布、下架、取消分享、停用机构 / 账户等高风险操作弹二次确认。 |
| 列表分页 | 两个后台所有列表（含详情页内嵌列表）统一分页，**每页最多展示 10 条**；**超过 10 条才显示分页器并真实翻页**（第 11 条起进第 2 页，以此类推），**≤10 条不显示分页器**；分页器展示「共 N 条」+ 页码（含首、尾页与当前页邻近页，页数过多时以省略号收起）；搜索 / 筛选改变结果数时自动回到第 1 页。 |
| 列表导出（统一标准） | **【0615-7】明确**。两后台所有「导出」按钮统一规则：**格式 = Excel（.xlsx）**；**范围 = 当前筛选 + 排序后的全部结果**（不是仅当前页，也不是无视筛选的全量）；**字段 = 列表可见列 + 必要的完整字段**（如完整订单号 / ID、精确到秒的时间、用户标识等，不脱敏截断）；**单次上限 1 万条**，超出提示「请缩小筛选范围或分批导出」；**文件名 = 模块名 + 筛选摘要 + 时间戳**（如 `订单管理_近7天_已支付_20260616.xlsx`）；导出为前端即时生成、不经审批。各模块具体导出字段见 PRD「导出规则」节。 |
| 指标说明问号 + tooltip | 后台 KPI / 用量指标名称**紧随其后**置浅灰小问号，悬浮展示统计规则与统计区间、移开即消失；**tooltip 自适应位置**——行内最后一张卡片改为向左展开，避免被页面右缘遮挡。 |
| 后台下拉选择器（两端通用） | **【0610】** 统一下拉框宽度为与同表单其他控件一致（通查机构后台 / 平台超管所有下拉，不符的统一整改）；选中后回显文字**不使用主题色**、与普通 UI 文本色一致（仅反映数据变化）。 |
| 后台侧栏收起态（两端通用） | **【0610】** 左侧菜单收起后，鼠标悬浮浮出的菜单名小面板**层级修正**，不再被右侧表单内容遮盖。 |
| 指标说明 tooltip 层级（两端通用） | **【0613-2】新增。** 修复 KPI / 用量指标的问号悬浮面板被**左侧菜单栏压盖**的 bug（主控台「累计退款」、机构详情用量看板「配额用量」等处复现）；通查两后台所有指标 tooltip 统一抬升层级 / 溢出可见。 **【0614】按实测截图复查并彻底修掉「累计退款」等指标 tooltip 仍被左侧菜单压盖的问题（定位 `.kpi`/`.dash-section` 的 `overflow` 与侧栏 z 层级）。** |
| 后台表单必填项红星（两端通用） | **【0613-2】新增。** 通查两后台所有表单，为必填字段补「红色 ＊」：Agent（名称 / TTS 参考音 / 头像）、KP（名称 / 关联 Agent）、创建机构（机构名称）、创建账户（账户名称 / 姓名 / 所属机构 / 角色）、新建角色（角色名称）、客服配置（客服二维码）等；选填项（上级机构 / 套餐 / 微信配置整组 / 回答 Prompt）不标。 |
| 后台表单保存按钮位置（两端通用） | **【0613-2】新增。** 保存按钮位置不强制单一规范，按各表单填写流程与用户习惯就近放在最自然处；无明显倾向时用兜底默认（表单卡片下方左对齐）；两后台统一此原则，消除同类表单按钮忽左忽右。 |
| 后台列表操作列固定（两端通用） | **【0613】新增。** 两个后台所有列表（含详情内嵌列表）的「操作」列**固定在表格右侧**（sticky），中间数据字段可左右横向滚动，避免运营一直右滑才够到操作。通查机构后台 / 平台超管全部含操作列的列表统一生效。 **【0613-2】固定的「操作」列底色加深一档，与可横滚的数据列做视觉区分。** **【0614】操作列视觉区分由"加深底色块"改为更**柔和过渡**——左侧发丝线 + 极淡渐隐 / 磨砂底，圆润自然、不与左侧割裂。** **【0614b】最终样式（按截图）：固定操作列**底色与表格一致**（白底 / hover 暖底，不再用任何色块），仅靠**左侧一道 1px 细线 + 柔和左阴影**区分，横滚时数据从其下方滑过。** **【0615-5】通查整改、统一执行标准：两后台所有含「操作」列的列表（含详情内嵌、订阅记录、加油包抽屉）一律走共享 `.dg-op` 标准——① 即使列表无需横滚，操作列左侧 1px 浅灰边框线 + 左阴影也**常驻可见**；② **横向滚动时操作列为不透明白底**，下方数据列文字不透出；③ 左阴影**适度加重**，更清晰区分固定列与可横滚区。以「机构管理」为达标基准，订阅记录等全部对齐同一标准。** **【0615-5b】进一步修正：**表格未横向溢出（内容没撑满、无需滚动）时，固定操作列的浅灰线 + 阴影原本几乎看不见（1px 15% 灰线投在白底上、柔光阴影无内容衬托）。改为左侧 1px **浅灰线（不透明度提到 .2、更清晰）+ 贴边 1px 硬阴影线 + 外扩柔光**，**无论是否左右滑动都常驻清晰可见**；横滚时同样压在数据列之上（不透字）。两后台全部非卡片列表（机构后台订单管理、平台后台机构管理 / 订阅订单 / 全域订单 / 全域用户等）一并生效。** **【0615-5c】根因订正：上面 5b 的 `box-shadow` 方案其实**不生效**——`.tbl` 为 `border-collapse:collapse`，collapse 边框模型下单元格的 `box-shadow` 在 Chrome **不渲染**（这才是订阅订单 / 订单管理"操作列没有灰线"的真因，窄表 / 未横滚时尤其明显）。改为**不用 box-shadow**：`border-left` 画 1px 浅灰线（不透明度 .22）+ **伪元素 `::before` 渐变**（紧贴操作列左侧、向左投 12px 柔光）。伪元素不受 collapse 影响，无论是否左右滑动都常驻清晰可见、横滚时压在数据列之上。两后台全部非卡片列表一并生效。** **【0615-5d】定稿（姐姐确认视觉方向 + 修两个 BUG）：① 视觉——**去掉硬边框线**，只留一道**很轻的柔和左阴影**（伪元素 `::before` 渐变 .09 / 10px，"看出层次即可"，不再厚重）；线 + 阴影并存显脏，去线更干净现代。② **BUG：横滚时操作列右侧露出被滑动的数据**——根因是 `.tbl-wrap` 滚动容器 `padding-right:10px`，固定列 sticky `right:0` 贴内容区右缘、右边留 ~11px 缝，横滚时下方数据从缝透出；用 `::after` 同底色盖板（宽度 = 右内边距 10px、正好盖到 clientWidth 边缘）盖住该缝，横滚时彻底看不见底下数据。③ 盖板宽度严格 = 内边距、**不越界**，避免给"本可放下"的表凭空加 4px 横向滚动条。三端 build✓、零 console error。** |

## 附：指标体系总表（机构后台）

> **【0614 整体重划】** 钱归钱、用户归用户、提问归提问、KP 归 KP。
> - **主控台 = 关键指标驾驶舱**：跨域少量关键指标 + 1 张总趋势图，一眼看全局。
> - **数据看板 = 分主题深钻**：去掉「总览」Tab，按域分 用户分析 / 提问分析 / 营收分析 / 热门 KP 四个 Tab；顶部统一时间区间筛选（今日 / 7 日 / 30 日 / 自定义），全 Tab 与图表联动；整页支持导出。
> - **实时 vs 区间**：标「实时」＝累计 / 存量快照，不随时间筛选；标「区间」＝随所选时间区间真联动（假数据按今日 / 7 日 / 30 日给不同值，不写死）。

### 主控台
| 段 | 指标 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| 实时总览 | 累计 GMV | 实时 | 总营收 |
| 实时总览 | 累计退款 / 退款率 | 实时 | 售后与资金健康度 |
| 实时总览 | 净 GMV（累计扣退款） | 实时 | 实际到账净收入（**【0614】新增**） |
| 实时总览 | 当前会员数 | 实时 | 商业化基本盘 |
| 实时总览 | 累计注册用户 | 实时 | 用户规模 |
| 经营分析 | 活跃用户 / 新增会员 / 区间 GMV / 区间提问数 + 活跃与会员趋势图 | 区间 | 经营动态 |

### 数据看板 · Tab 1 用户分析（用户域）
| 指标 | 公式 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| DAU / WAU / MAU | 周期内有效行为去重用户数 | 实时（固定窗口快照） | 粘性分层 |
| 新增用户数 | 区间内首次注册数 | 区间 | 拉新效果 |
| 次日 / 7 日 / 30 日留存率 | 新增用户在 N 日后仍活跃占比 | 区间 | 产品价值是否站得住 |
| 来源分布 | 扫码进入 vs 直接访问 | 区间 | 渠道效果 |
| 地区分布 / 性别分布 | 按地区、性别分组的用户占比 | 区间 | 用户画像与精细化运营 |

### 数据看板 · Tab 2 提问分析（提问域）
| 指标 | 公式 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| 总提问数 | COUNT(提问) | 区间 | 使用强度 |
| 人均提问数 | 总提问数 / 活跃用户 | 区间 | 单用户深度 |
| 平均会话轮次 | 总提问数 / 总会话数 | 区间 | 对话沉浸度 |
| 答案点赞率 / 答案反馈率 | 点赞数 ÷ 答案数；反馈数 ÷ 答案数 | 区间 | 答案质量摘要（明细在「数据中心·答案反馈」菜单）（**【0614】移入**） |
| 提问量趋势 | 每日提问条数 | 区间 | 提问走势（**【0614】自原总览 Tab 移入**） |
| 按 Agent 提问分布 | 每个 Agent 接收提问占比 | 区间 | Agent 配置 / 路由质量 |
| 提问领域分布 | 按 KP / 领域聚合的提问占比 | 区间 | 用户最关心的领域 |
| 提问关键词云 | 高频提问关键词，hover 显示该词区间提问数 | 区间 | 运营 / 商业化分析 |

### 数据看板 · Tab 3 营收分析（钱域 · 分三小节）
| 小节 | 指标 | 公式 | 实时 / 区间 | 业务意义 |
|---|---|---|---|---|
| ① 收入与转化 | 区间 GMV | SUM(区间已支付订单金额) | 区间 | 区间营收 |
| ① 收入与转化 | 付费用户数 / 付费转化率 | 区间付费用户 / 区间活跃用户 | 区间 | 商业化核心 |
| ① 收入与转化 | ARPPU | 区间收入 / 区间付费用户数 | 区间 | 单付费用户产值 |
| ① 收入与转化 | 续费率 | 到期续费数 / 到期会员数 | 区间 | 长期价值 |
| ② 退款 | 退款金额 / 退款率 / 退款订单数 / 净 GMV | 见各指标 | 区间 | 售后与净收入 |
| ③ 转化漏斗 | 受限内容触发率 | 触发付费墙次数 / 总提问数 | 区间 | 付费墙强度（漏斗入口，**【0614】自提问 Tab 移入**） |
| ③ 转化漏斗 | 会员漏斗 | 看到会员页 → 点击购买 → 完成支付（%） | 区间 | 找漏斗瓶颈 |
| ③ 转化漏斗 | 永享转化 | 触发永享墙 → 完成购买（%） | 区间 | 永享定价是否合理 |

### 数据看板 · Tab 4 热门 KP（KP 域）
| 指标 | 取数 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| 被提问数 TOP 10 KP | 按 KP 维度统计提问 | 区间 | 真正被使用的内容 |
| 付费转化贡献 TOP 10 KP | 通过该 KP 触发的会员 / 永享订单数 | 区间 | 营销价值高的内容 |
| 永享购买 TOP 10 KP | 按 KP 永享订单数 | 区间 | 永享潜力 |

---

## 附：用量看板指标表（平台超管 · 机构详情）

> 平台视角看单个机构运营健康度，支持时间区间筛选 + 导出。

| 维度 | 指标 |
|---|---|
| 活跃度 | 该机构 DAU / WAU / MAU、累计 C 端用户数、新增 C 端用户数 |
| 内容 | KP 总数（已发布 / 未发布 / 已下架）、累计提问数 |
| 商业化 | 累计 GMV、当前会员数、永享订单数、付费转化率、**退款金额 / 退款率（【0614】新增）** |
| 配额用量（【0614】） | KP 数、存储、本月 Token 的「已用 / 上限」进度（Token 统一「亿」）；达 70%/80%/90%/95% 标注已触发预警阈值并示意已向机构联系人发送预警短信 |
| LLM 用量（仅平台默认 LLM） | tokens 用量、调用次数、平均响应时长 |

---

## 附：指标体系总表（平台超管）

> **【0614】** 平台域同样"各归各位"：主控台只放关键指标（生态 + 规模 + 钱），LLM 成本归「模型用量」，用户 / 订单 / KP 明细走「全域」列表。

### 主控台（平台超管）
| 段 | 指标 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| 实时总览 | 入驻机构数 | 实时 | 平台生态规模 |
| 实时总览 | 累计用户 | 实时 | 全平台用户规模 |
| 实时总览 | 累计 GMV | 实时 | 全平台总营收 |
| 实时总览 | 净 GMV（扣退款） | 实时 | 全平台净收入（**【0614】新增**，tooltip 注明退款额 · 率） |
| 实时总览 | 提问总量 | 实时 | 全平台使用强度 |
| 经营分析 | 活跃用户 / 新增会员 / 区间 GMV / 区间提问数 + 提问量趋势 | 区间 | 经营动态（支持全部 / 指定机构筛选） |

### 模型用量（平台默认 LLM）
| 段 | 指标 | 实时 / 区间 | 业务意义 |
|---|---|---|---|
| 实时总览 | 累计 tokens / 累计调用次数 | 实时 | 平台开通至今 LLM 总用量 |
| 经营分析 | 区间 tokens / 区间调用次数 / 平均响应 | 区间 | LLM 成本与性能（**【0614】修复联动**） |
| 经营分析 | 全平台总量趋势 + Top 机构 token 排行 | 区间 | 趋势与机构分布（**【0614】真联动；「当期」改按所选时间显示**） |

---

## 附：指标数值单位规范（【0614b】两后台通用 · 开发依据）

> 调研结论：中文后台看板的行业规范是**中文万进制（个 / 万 / 亿）**，不是英文千进制（K / M）。四位数（如 1,200）直接显示「1,200」，到 1 万才缩写。Token 虽行业按「每百万（M）」计价，但本产品**前台展示统一中文万进制**（与配额额度 2亿 / 10亿 一致）。前端实现见 `packages/ui-admin/src/format.ts` 的 `fmtCn`。

### 数值缩写规则（所有指标统一）
| 数量级 | 显示 | 示例 |
|---|---|---|
| `\| n \| < 10,000` | 千分位原值 | 8,204 / 1,240 |
| `10,000 ≤ \| n \| < 1 亿` | 1 位小数 + 万（去尾零） | 12,000 → 1.2万；255,920 → 25.6万 |
| `\| n \| ≥ 1 亿` | 2 位小数 + 亿（去尾零） | 105,000,000 → 1.05亿 |
| 金额 | `¥` 前缀 + 上规则 | 862,000 → ¥86.2万 |
| 比率 / 时长 / 容量 | 不缩写 | 6.9% / 1.8s / 100 GB |

界面上每个 KPI 数值**后缀显示单位**（小灰字），并在主控台 / 数据看板 / 模型用量 / 机构详情用量看板**页脚显示单位规范说明 + 典型案例**（如「1.2万 = 12,000、1.05亿 = 105,000,000」）。**单位升级直接解决平台主控台「提问总量」等大数撑爆卡片的问题**（3,200,000 → 320万 条）。

### 各指标单位对照
| 指标类 | 指标 | 单位 |
|---|---|---|
| 金额 | 累计 / 净 / 区间 GMV、退款金额、ARPPU | 元（¥，万 / 亿缩写） |
| 机构 | 入驻机构数 | 家 |
| 用户 | 累计用户 / 注册、当前会员、新增会员 / 用户、付费用户、活跃用户、DAU / WAU / MAU | 人 |
| 提问 | 提问总量 / 总提问 / 区间提问数 | 条 |
| 提问 | 人均提问 | 条/人（小数） |
| 提问 | 平均会话轮次 | 轮（小数） |
| 调用 | 调用次数 | 次 |
| 订单 | 退款订单数、永享订单 | 单 |
| 内容 | KP 数 / KP 总数 | 个 |
| Token | 累计 / 区间 tokens、Top 机构 token、月度额度 / 已用 | token（万 / 亿） |
| 存储 | 已用 / 上限 | GB（≥1024 GB 升 TB） |
| 比率 | 退款率 / 付费转化率 / 留存率 / 续费率 / 受限触发率 / 答案点赞率 / 反馈率 | %（1 位小数） |
| 性能 | 平均响应 | s |
